import { Intake } from "@/components/intake/Intake";

export const metadata = {
  title: "Build your plan",
  description: "A 60-second, tailored plan — no chatbot, no typing.",
};

export default function StartPage() {
  return <Intake />;
}
