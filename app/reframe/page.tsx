import { Reframe } from "@/components/practice/Reframe";

export const metadata = {
  title: "Reframe a thought",
  description: "Catch a heavy thought and practice talking back to it.",
};

export default function ReframePage() {
  return <Reframe />;
}
